# Telegram Bot с MongoDB

Telegram бот с интеграцией MongoDB для хранения данных пользователей.

## 📚 Документация

- **[Инструкция по развертыванию на сервере](DEPLOYMENT.md)** - Полная инструкция по развертыванию бота на сервере с MongoDB
- **[Настройка MongoDB](MONGODB_SETUP.md)** - Инструкция по установке и настройке MongoDB

## 🚀 Быстрый старт

### Требования

- Node.js (версия 18 или выше)
- MongoDB (локально или MongoDB Atlas)
- Telegram Bot Token (получите у [@BotFather](https://t.me/BotFather))

### Установка

1. **Клонируйте репозиторий или скачайте файлы**

2. **Установите зависимости:**
```bash
npm install
```

3. **Создайте файл `.env` в корне проекта:**
```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
MONGODB_URI=mongodb://localhost:27017/cryptocheck
```

   Для MongoDB Atlas используйте:
```env
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/cryptocheck?retryWrites=true&w=majority
```

4. **Запустите бота:**
```bash
npm start
```

   Или для разработки с автоперезагрузкой:
```bash
npm run dev
```

## 📁 Структура проекта

```
qodeq_cryptocheck/
├── bot.js              # Основной файл бота
├── config/
│   └── database.js    # Конфигурация MongoDB
├── models/
│   ├── Wallet.js       # Модель кошелька
│   └── BalanceHistory.js # Модель истории балансов
├── services/
│   └── balanceChecker.js # Сервис проверки балансов
├── package.json        # Зависимости проекта
├── .env               # Переменные окружения (не в git)
├── .gitignore        # Игнорируемые файлы
└── README.md         # Документация
```

## 🔧 Настройка

### Получение Telegram Bot Token

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в файл `.env`

### Настройка MongoDB

#### Локальная установка MongoDB

1. Установите MongoDB на ваш компьютер
2. Запустите MongoDB сервис
3. Используйте строку подключения: `mongodb://localhost:27017/cryptocheck`

#### MongoDB Atlas (облачный сервис)

1. Зарегистрируйтесь на [MongoDB Atlas](https://www.mongodb.com/cloud/atlas)
2. Создайте бесплатный кластер
3. Создайте пользователя базы данных
4. Получите строку подключения и добавьте в `.env`

## 📝 Доступные команды бота

### Основные команды
- `/start` - Начать работу с ботом
- `/help` - Показать список команд

### Кошельки
- `/addwallet` - Добавить кошелек
- `/wallets` - Список ваших кошельков
- `/wallet <id>` - Информация о кошельке
- `/checkbalance <id>` - Проверить баланс кошелька
- `/checkall` - Проверить балансы всех ваших кошельков
- `/balancehistory <id>` - История балансов кошелька
- `/deletewallet <id>` - Удалить кошелек
- `/searchwallet <адрес>` - Найти кошелек по адресу

### Статистика
- `/stats` - Статистика бота

## 🗄️ Модели данных

### Wallet (Кошелек)

- `project` - Название проекта
- `user_id` - ID пользователя
- `wallet_destination` - Адрес кошелька (уникальный)
- `balance` - Текущий баланс
- `lastBalanceCheck` - Дата последней проверки баланса
- `createdAt` - Дата создания
- `updatedAt` - Дата обновления

### BalanceHistory (История балансов)

- `wallet_id` - Ссылка на кошелек
- `wallet_destination` - Адрес кошелька
- `balance` - Общий баланс
- `balanceTRX` - Баланс TRX
- `balanceUSDT` - Баланс USDT (TRC20)
- `checkedAt` - Дата проверки

## 🔨 Разработка

### Добавление новых команд

Откройте `bot.js` и добавьте обработчик команды:

```javascript
bot.onText(/\/новаякоманда/, async (msg) => {
  const chatId = msg.chat.id;
  await bot.sendMessage(chatId, 'Ответ на команду');
});
```

### Добавление новых моделей

Создайте новый файл в папке `models/` по аналогии с `User.js`.

## 📦 Зависимости

- `node-telegram-bot-api` - Библиотека для работы с Telegram Bot API
- `mongoose` - ODM для MongoDB
- `dotenv` - Загрузка переменных окружения

## 💰 Проверка балансов

Бот поддерживает проверку балансов криптокошельков через API:

- **TRON (TRC20)** - Полная поддержка через TronGrid API
  - Проверка баланса TRX
  - Проверка баланса USDT (TRC20)
- **Ethereum** - В разработке
- **Bitcoin** - В разработке

### Использование

1. Добавьте кошелек командой `/addwallet`
2. Проверьте баланс командой `/checkbalance <id>`
3. Просмотрите историю балансов командой `/balancehistory <id>`

### TronGrid API Key (опционально)

Для увеличения лимитов запросов можно добавить API ключ TronGrid:
1. Зарегистрируйтесь на https://www.trongrid.io/
2. Получите API ключ
3. Добавьте в `.env`: `TRONGRID_API_KEY=your_key`

## ⚠️ Важные замечания

1. **Никогда не публикуйте файл `.env`** - он содержит секретные данные
2. Убедитесь, что MongoDB запущена перед запуском бота
3. Для продакшена используйте переменные окружения сервера вместо `.env`
4. TronGrid API имеет лимиты запросов (можно увеличить с API ключом)

## 🐛 Решение проблем

### Бот не отвечает

- Проверьте правильность токена в `.env`
- Убедитесь, что бот запущен (`npm start`)

### Ошибка подключения к MongoDB

- Проверьте, что MongoDB запущена
- Проверьте правильность строки подключения в `.env`
- Для MongoDB Atlas проверьте настройки firewall (добавьте IP 0.0.0.0/0 для тестирования)

## 📄 Лицензия

ISC

