# 🔒 Защита MongoDB от атак

## ⚠️ Что произошло?

Ваша MongoDB была скомпрометирована. Злоумышленники получили доступ к незащищенной базе данных и:
- Зашифровали или удалили ваши данные
- Оставили сообщение с требованием выкупа

**ВАЖНО: НЕ ПЛАТИТЕ ВЫКУП!** Это не гарантирует возврат данных.

---

## 🚨 Немедленные действия

### 1. Отключите MongoDB от интернета

```bash
# Остановите MongoDB
sudo systemctl stop mongod

# Или если используете MongoDB Atlas - измените Network Access
# Удалите все IP адреса кроме вашего
```

### 2. Проверьте, что осталось

```bash
# Подключитесь к MongoDB
mongosh

# Проверьте базы данных
show dbs

# Проверьте коллекции
use cryptocheck
show collections

# Проверьте данные
db.wallets.countDocuments()
db.balancehistories.countDocuments()
```

### 3. Создайте резервную копию (если данные еще есть)

```bash
# Создайте бэкап
mongodump --uri="mongodb://localhost:27017/cryptocheck" --out=/backup/emergency_$(date +%Y%m%d_%H%M%S)

# Или для MongoDB Atlas
mongodump --uri="mongodb+srv://username:password@cluster.mongodb.net/cryptocheck" --out=/backup/emergency_$(date +%Y%m%d_%H%M%S)
```

---

## 🛡️ Защита MongoDB

### Вариант 1: MongoDB Atlas (РЕКОМЕНДУЕТСЯ)

**Преимущества:**
- Встроенная защита
- Автоматические бэкапы
- Мониторинг безопасности
- Ограничение доступа по IP

**Настройка безопасности:**

1. **Создайте пользователя с ограниченными правами:**
   - Database Access → Add New Database User
   - Username: `cryptobot`
   - Password: **сильный пароль** (минимум 16 символов, буквы, цифры, символы)
   - Database User Privileges: **Read and write to any database** (или только к cryptocheck)

2. **Настройте Network Access:**
   - Network Access → Add IP Address
   - **НЕ используйте 0.0.0.0/0** (доступ отовсюду)
   - Добавьте только IP вашего сервера
   - Или используйте IP whitelist

3. **Включите шифрование:**
   - Security → Encryption at Rest (если доступно)
   - Используйте TLS/SSL для подключений

4. **Настройте мониторинг:**
   - Alerts → включите уведомления о подозрительной активности

### Вариант 2: Локальная MongoDB с защитой

#### 1. Создайте пользователя с паролем

```bash
# Подключитесь к MongoDB
mongosh

# Переключитесь на admin базу
use admin

# Создайте администратора
db.createUser({
  user: "admin",
  pwd: "STRONG_PASSWORD_HERE",  # Замените на сильный пароль!
  roles: [ { role: "userAdminAnyDatabase", db: "admin" }, "readWriteAnyDatabase" ]
})

# Создайте пользователя для приложения
use cryptocheck
db.createUser({
  user: "cryptobot",
  pwd: "STRONG_PASSWORD_HERE",  # Замените на сильный пароль!
  roles: [ { role: "readWrite", db: "cryptocheck" } ]
})

exit
```

#### 2. Включите аутентификацию

```bash
# Отредактируйте конфигурацию MongoDB
sudo nano /etc/mongod.conf
```

Добавьте или раскомментируйте:
```yaml
security:
  authorization: enabled
```

Перезапустите MongoDB:
```bash
sudo systemctl restart mongod
```

#### 3. Настройте firewall

```bash
# Ubuntu/Debian
sudo ufw enable
sudo ufw allow from YOUR_SERVER_IP to any port 27017
sudo ufw deny 27017

# Или полностью закройте порт от внешнего доступа
sudo ufw deny 27017
```

#### 4. Измените порт MongoDB (опционально)

```bash
# В /etc/mongod.conf
net:
  port: 27018  # Измените на другой порт
```

---

## 🔐 Обновление .env файла

После настройки безопасности обновите `.env`:

### Для MongoDB Atlas:
```env
MONGODB_URI=mongodb+srv://cryptobot:STRONG_PASSWORD@cluster0.xxxxx.mongodb.net/cryptocheck?retryWrites=true&w=majority
```

### Для локальной MongoDB:
```env
MONGODB_URI=mongodb://cryptobot:STRONG_PASSWORD@localhost:27017/cryptocheck?authSource=cryptocheck
```

**ВАЖНО:** Замените `STRONG_PASSWORD` на реальный сильный пароль!

---

## 📋 Чек-лист безопасности

- [ ] MongoDB требует аутентификацию
- [ ] Создан пользователь с ограниченными правами (не admin)
- [ ] Используется сильный пароль (16+ символов)
- [ ] Порт 27017 закрыт от внешнего доступа (firewall)
- [ ] В MongoDB Atlas настроен IP whitelist (не 0.0.0.0/0)
- [ ] Регулярные бэкапы настроены
- [ ] Мониторинг безопасности включен
- [ ] Обновлен `.env` файл с новыми учетными данными

---

## 🔄 Восстановление данных

### Если у вас есть бэкап:

```bash
# Восстановление из бэкапа
mongorestore --uri="mongodb://cryptobot:password@localhost:27017/cryptocheck?authSource=cryptocheck" /backup/path/to/backup/cryptocheck
```

### Если данных нет:

1. **Проверьте логи MongoDB:**
   ```bash
   sudo journalctl -u mongod -n 100
   ```

2. **Проверьте, не осталось ли резервных копий:**
   - Проверьте автоматические бэкапы MongoDB Atlas
   - Проверьте локальные бэкапы на сервере

3. **Если данных нет - начните заново:**
   - Данные нужно будет добавить заново через бота
   - Это займет время, но безопаснее чем платить выкуп

---

## 🚫 Что НЕ делать

- ❌ **НЕ платите выкуп** - нет гарантии возврата данных
- ❌ **НЕ оставляйте MongoDB без пароля**
- ❌ **НЕ используйте 0.0.0.0/0 в Network Access** (MongoDB Atlas)
- ❌ **НЕ используйте порт 27017 без firewall**
- ❌ **НЕ используйте слабые пароли**

---

## 📊 Мониторинг безопасности

### Проверка подключений:

```bash
# Просмотр активных подключений
mongosh --eval "db.currentOp()"

# Просмотр пользователей
mongosh --eval "db.getUsers()"
```

### Настройка алертов (MongoDB Atlas):

1. Alerts → Create Alert
2. Настройте уведомления на:
   - Необычные подключения
   - Изменения в данных
   - Удаление коллекций

---

## 🔧 Быстрая настройка безопасности (MongoDB Atlas)

1. **Database Access:**
   - Удалите всех пользователей кроме нужных
   - Создайте нового пользователя с сильным паролем
   - Дайте только права `readWrite` на базу `cryptocheck`

2. **Network Access:**
   - Удалите `0.0.0.0/0`
   - Добавьте только IP вашего сервера
   - Или используйте Private Endpoint

3. **Security:**
   - Включите MFA для аккаунта Atlas
   - Включите Audit Logging (если доступно)

4. **Backup:**
   - Включите Cloud Backup
   - Настройте автоматические бэкапы

---

## 📞 Дополнительные ресурсы

- [MongoDB Security Checklist](https://www.mongodb.com/docs/manual/administration/security-checklist/)
- [MongoDB Atlas Security](https://www.mongodb.com/docs/atlas/security/)
- [MongoDB Best Practices](https://www.mongodb.com/docs/manual/administration/production-notes/)

---

## ⚡ Быстрое исправление (если используете MongoDB Atlas)

1. Зайдите в MongoDB Atlas
2. **Database Access** → удалите всех пользователей → создайте нового с сильным паролем
3. **Network Access** → удалите `0.0.0.0/0` → добавьте только IP вашего сервера
4. Обновите `.env` с новыми учетными данными
5. Перезапустите бота: `pm2 restart cryptobot`

**Ваша база данных будет защищена!**

