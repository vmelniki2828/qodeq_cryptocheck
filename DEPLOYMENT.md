# 🚀 Инструкция по развертыванию бота на сервере

## 📋 Содержание
1. [Подготовка сервера](#подготовка-сервера)
2. [Установка Node.js](#установка-nodejs)
3. [Настройка MongoDB](#настройка-mongodb)
4. [Развертывание бота](#развертывание-бота)
5. [Настройка автозапуска](#настройка-автозапуска)
6. [Мониторинг и логи](#мониторинг-и-логи)

---

## 🖥️ Подготовка сервера

### Требования к серверу:
- **ОС**: Ubuntu 20.04+ / Debian 11+ / CentOS 8+ (рекомендуется Ubuntu)
- **RAM**: минимум 512MB (рекомендуется 1GB+)
- **Диск**: минимум 5GB свободного места
- **Интернет**: стабильное подключение

### Обновление системы (Ubuntu/Debian):
```bash
sudo apt update
sudo apt upgrade -y
```

---

## 📦 Установка Node.js

### Вариант 1: Через NodeSource (рекомендуется)
```bash
# Установка Node.js 20.x LTS
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# Проверка версии
node --version  # Должно быть v20.x.x или выше
npm --version
```

### Вариант 2: Через nvm (Node Version Manager)
```bash
# Установка nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
source ~/.bashrc

# Установка Node.js
nvm install 20
nvm use 20
nvm alias default 20
```

---

## 🗄️ Настройка MongoDB

### Вариант 1: MongoDB Atlas (Облачный сервис) - РЕКОМЕНДУЕТСЯ

**Преимущества:**
- Не требует установки на сервере
- Автоматические бэкапы
- Масштабируемость
- Бесплатный план M0 (512MB)

**Шаги:**

1. **Регистрация:**
   - Перейдите на https://www.mongodb.com/cloud/atlas/register
   - Создайте бесплатный аккаунт

2. **Создание кластера:**
   - Войдите в Atlas
   - Нажмите "Build a Database"
   - Выберите бесплатный план (M0)
   - Выберите регион (ближайший к серверу)
   - Нажмите "Create"

3. **Создание пользователя:**
   - Раздел "Database Access" → "Add New Database User"
   - Username: `cryptobot` (или любое другое)
   - Password: создайте надежный пароль (сохраните!)
   - Database User Privileges: "Atlas admin" или "Read and write to any database"

4. **Настройка сетевого доступа:**
   - Раздел "Network Access" → "Add IP Address"
   - Для продакшена: добавьте IP вашего сервера
   - Для тестирования: "Allow Access from Anywhere" (0.0.0.0/0)
   - Нажмите "Confirm"

5. **Получение строки подключения:**
   - Раздел "Database" → "Connect" → "Connect your application"
   - Выберите "Node.js" и версию "5.5 or later"
   - Скопируйте строку подключения
   - Замените `<password>` на ваш пароль
   - Замените `<dbname>` на `cryptocheck`

**Пример строки подключения:**
```
mongodb+srv://cryptobot:your_password@cluster0.xxxxx.mongodb.net/cryptocheck?retryWrites=true&w=majority
```

### Вариант 2: Локальная установка MongoDB

#### Ubuntu/Debian:
```bash
# Импорт ключа MongoDB
curl -fsSL https://www.mongodb.org/static/pgp/server-7.0.asc | sudo gpg -o /usr/share/keyrings/mongodb-server-7.0.gpg --dearmor

# Добавление репозитория
echo "deb [ arch=amd64,arm64 signed-by=/usr/share/keyrings/mongodb-server-7.0.gpg ] https://repo.mongodb.org/apt/ubuntu jammy/mongodb-org/7.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-7.0.list

# Установка
sudo apt update
sudo apt install -y mongodb-org

# Запуск и автозапуск
sudo systemctl start mongod
sudo systemctl enable mongod

# Проверка статуса
sudo systemctl status mongod
```

#### Настройка MongoDB:
```bash
# Подключение к MongoDB
mongosh

# Создание пользователя (внутри mongosh)
use cryptocheck
db.createUser({
  user: "cryptobot",
  pwd: "your_secure_password",
  roles: [{ role: "readWrite", db: "cryptocheck" }]
})
exit
```

**Строка подключения:**
```
mongodb://cryptobot:your_secure_password@localhost:27017/cryptocheck
```

---

## 🤖 Развертывание бота

### 1. Создание директории проекта
```bash
# Создаем директорию для бота
mkdir -p ~/cryptobot
cd ~/cryptobot
```

### 2. Загрузка файлов проекта

#### Вариант A: Через Git (если проект в репозитории)
```bash
# Клонирование репозитория
git clone https://github.com/your-username/qodeq_cryptocheck.git .
# или
git clone https://github.com/your-username/qodeq_cryptocheck.git ~/cryptobot
cd ~/cryptobot
```

#### Вариант B: Через SCP (с локального компьютера)
```bash
# На локальном компьютере (Windows PowerShell или Linux/Mac)
scp -r ./qodeq_cryptocheck user@your-server-ip:~/cryptobot

# На сервере
cd ~/cryptobot
```

#### Вариант C: Через SFTP или файловый менеджер
- Используйте FileZilla, WinSCP или другой SFTP клиент
- Загрузите все файлы проекта в `~/cryptobot`

### 3. Установка зависимостей
```bash
cd ~/cryptobot
npm install
```

### 4. Создание файла .env
```bash
nano .env
```

**Содержимое файла .env:**
```env
# Telegram Bot Token (получите у @BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# MongoDB Connection String
# Для MongoDB Atlas:
MONGODB_URI=mongodb+srv://cryptobot:your_password@cluster0.xxxxx.mongodb.net/cryptocheck?retryWrites=true&w=majority

# Или для локальной MongoDB:
# MONGODB_URI=mongodb://cryptobot:your_password@localhost:27017/cryptocheck

# TronGrid API Key (опционально, для увеличения лимитов запросов)
# TRONGRID_API_KEY=your_tron_grid_api_key_here
```

**Сохранение в nano:**
- `Ctrl + O` - сохранить
- `Enter` - подтвердить
- `Ctrl + X` - выйти

### 5. Тестовый запуск
```bash
npm start
```

Если все настроено правильно, вы увидите:
```
🔄 Попытка подключения к MongoDB...
✅ MongoDB подключена успешно
📊 База данных: cryptocheck
⏰ Автоматическая проверка балансов настроена: 6:00 и 15:00 (МСК)
🤖 Бот запущен и готов к работе!
```

**Остановка:** `Ctrl + C`

---

## 🔄 Настройка автозапуска

### Установка PM2 (Process Manager)
```bash
# Глобальная установка PM2
sudo npm install -g pm2

# Проверка установки
pm2 --version
```

### Запуск бота через PM2
```bash
cd ~/cryptobot

# Запуск бота
pm2 start bot.js --name cryptobot

# Сохранение конфигурации PM2
pm2 save

# Настройка автозапуска при перезагрузке сервера
pm2 startup
# Выполните команду, которую выведет PM2 (обычно что-то вроде):
# sudo env PATH=$PATH:/usr/bin pm2 startup systemd -u your_user --hp /home/your_user
```

### Полезные команды PM2:
```bash
# Просмотр статуса
pm2 status

# Просмотр логов
pm2 logs cryptobot

# Просмотр логов в реальном времени
pm2 logs cryptobot --lines 50

# Перезапуск бота
pm2 restart cryptobot

# Остановка бота
pm2 stop cryptobot

# Удаление из PM2
pm2 delete cryptobot

# Мониторинг (CPU, память)
pm2 monit
```

---

## 📊 Мониторинг и логи

### Просмотр логов
```bash
# Все логи
pm2 logs cryptobot

# Только последние 100 строк
pm2 logs cryptobot --lines 100

# Логи без истории
pm2 logs cryptobot --nostream

# Очистка логов
pm2 flush
```

### Ротация логов (опционально)
PM2 автоматически ротирует логи, но можно настроить вручную:
```bash
pm2 install pm2-logrotate
pm2 set pm2-logrotate:max_size 10M
pm2 set pm2-logrotate:retain 7
```

### Мониторинг ресурсов
```bash
# Интерактивный мониторинг
pm2 monit

# Статистика
pm2 describe cryptobot
```

---

## 🔧 Дополнительные настройки

### Обновление бота
```bash
cd ~/cryptobot

# Если используете Git:
git pull origin main

# Обновление зависимостей (если изменились)
npm install

# Перезапуск бота
pm2 restart cryptobot
```

### Настройка файрвола (если нужен доступ к MongoDB)
```bash
# Ubuntu/Debian
sudo ufw allow 27017/tcp

# CentOS/RHEL
sudo firewall-cmd --permanent --add-port=27017/tcp
sudo firewall-cmd --reload
```

### Резервное копирование MongoDB

#### MongoDB Atlas:
- Автоматические бэкапы включены по умолчанию
- Можно настроить в разделе "Backup"

#### Локальная MongoDB:
```bash
# Создание бэкапа
mongodump --uri="mongodb://cryptobot:password@localhost:27017/cryptocheck" --out=/backup/$(date +%Y%m%d)

# Восстановление
mongorestore --uri="mongodb://cryptobot:password@localhost:27017/cryptocheck" /backup/20231115/cryptocheck
```

---

## ✅ Проверка работоспособности

1. **Проверка подключения к MongoDB:**
   ```bash
   pm2 logs cryptobot | grep MongoDB
   ```
   Должно быть: `✅ MongoDB подключена успешно`

2. **Проверка работы бота:**
   - Откройте Telegram
   - Найдите вашего бота
   - Отправьте команду `/start`
   - Бот должен ответить списком команд

3. **Проверка автоматической проверки:**
   ```bash
   pm2 logs cryptobot | grep "Автоматическая проверка"
   ```
   В 6:00 и 15:00 по МСК должны появляться логи проверки

---

## 🐛 Решение проблем

### Бот не запускается
```bash
# Проверьте логи
pm2 logs cryptobot --err

# Проверьте переменные окружения
cat .env

# Проверьте синтаксис
node -c bot.js
```

### Ошибка подключения к MongoDB
```bash
# Проверьте строку подключения
cat .env | grep MONGODB_URI

# Проверьте доступность MongoDB Atlas
ping cluster0.xxxxx.mongodb.net

# Для локальной MongoDB проверьте статус
sudo systemctl status mongod
```

### Бот не отвечает
- Проверьте правильность токена в `.env`
- Проверьте, что бот запущен: `pm2 status`
- Проверьте логи: `pm2 logs cryptobot`

### Высокое использование памяти
```bash
# Ограничение памяти для PM2
pm2 start bot.js --name cryptobot --max-memory-restart 500M
pm2 save
```

---

## 📝 Чек-лист развертывания

- [ ] Сервер подготовлен и обновлен
- [ ] Node.js установлен (версия 18+)
- [ ] MongoDB настроена (Atlas или локально)
- [ ] Проект загружен на сервер
- [ ] Зависимости установлены (`npm install`)
- [ ] Файл `.env` создан и заполнен
- [ ] Тестовый запуск успешен
- [ ] PM2 установлен
- [ ] Бот запущен через PM2
- [ ] Автозапуск настроен (`pm2 startup`)
- [ ] Бот отвечает в Telegram
- [ ] Логи проверены

---

## 🔐 Безопасность

1. **Не публикуйте файл `.env`** - он содержит секретные данные
2. **Используйте сильные пароли** для MongoDB
3. **Ограничьте сетевой доступ** к MongoDB (не используйте 0.0.0.0/0 в продакшене)
4. **Регулярно обновляйте** зависимости: `npm audit fix`
5. **Настройте бэкапы** базы данных
6. **Используйте firewall** для защиты сервера

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `pm2 logs cryptobot`
2. Проверьте статус: `pm2 status`
3. Проверьте документацию MongoDB: https://docs.mongodb.com/
4. Проверьте документацию PM2: https://pm2.keymetrics.io/docs/

---

**Успешного развертывания! 🚀**

